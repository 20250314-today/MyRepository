<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ApplyMapper">
<select id="selectAll" resultType="com.example.entity.Apply">
    select apply.* ,user.name as userName from `apply`
    left join user on apply.user_id=user.id
    <where>
        <if test="title != null and title != '' ">title like concat('%',#{title},'%')</if>
        <if test="userId != null ">and user_id =#{userId}</if>

    </where>
             order by id desc
</select>
    <insert id="insert">
        insert into `apply` (user_id,title,content,time,status,reason)
        values(#{userId},#{title},#{content},#{time},#{status},#{reason})
    </insert>
    <update id="updateById">
        update `apply` set title =#{title},content=#{content},
                          time=#{time},reason=#{reason},status=#{status}
        where id=#{id}
    </update>
</mapper>