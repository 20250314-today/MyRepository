<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.TeacherMapper">
<select id="selectAll" resultType="com.example.entity.Teacher">
    select * from `teacher`
    <where>
        <if test="name != null and name != '' ">name like concat('%',#{name},'%')</if>
        <if test="username != null and username != '' ">and username like concat('%',#{username},'%')</if>
        <if test="ids != null and ids !='' ">
            and id in
             <foreach collection="idsArr" open="(" close=")" separator="," item="id">#{id}</foreach>
        </if>
    </where>
             order by id desc
</select>
    <insert id="insert">
        insert into `teacher` (username,password,name,phone,email,role,avatar)
        values(#{username},#{password},#{name},#{phone},#{email},#{role},#{avatar})
    </insert>
    <update id="updateById">
        update `teacher` set username =#{username},password=#{password},
                          name=#{name},phone=#{phone},email=#{email},avatar=#{avatar}
        where id=#{id}
    </update>
</mapper>